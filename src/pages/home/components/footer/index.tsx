import { Update } from '../../../../components/update';
import './index.scss';

export const HomeFooter = () => {
    return (
        <div className="footer">
            <Update />
        </div>
    );
};
